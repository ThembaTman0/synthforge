spring:
  datasource:
    url: jdbc:h2:mem:synthforge-demo
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

synthforge:
  enabled-profiles:
    - dev
    - test
