<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.github.thembatman0</groupId>
    <artifactId>synthforge-parent</artifactId>
    <version>0.1.0</version>
  </parent>

  <artifactId>synthforge-spring</artifactId>
  <packaging>jar</packaging>

  <name>SynthForge Spring Boot Starter</name>
  <description>Spring Boot autoconfiguration and the @Seed annotation for
    SynthForge.</description>

  <!-- Explicit override: without this, flatten-maven-plugin derives a
       per-module url/scm by appending the artifactId as a path segment
       onto the parent's value (a convention for multi-repo layouts, not
       this single-repo one), producing broken URLs. -->
  <url>https://github.com/ThembaTman0/synthforge</url>
  <scm>
    <connection>scm:git:https://github.com/ThembaTman0/synthforge.git</connection>
    <developerConnection>scm:git:git@github.com:ThembaTman0/synthforge.git</developerConnection>
    <url>https://github.com/ThembaTman0/synthforge</url>
    <tag>HEAD</tag>
  </scm>

  <!-- @Seed annotation + Spring Boot autoconfiguration.
       See synthforge-v1-spec.md sections 6 and 9. -->

  <dependencies>
    <dependency>
      <groupId>io.github.thembatman0</groupId>
      <artifactId>synthforge-core</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-autoconfigure</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
      <optional>true</optional>
    </dependency>
  </dependencies>

  <!-- Publish plugins (source/javadoc/gpg/central-publish/flatten) live
       behind the "release" profile, not the default build: CI and every
       plain `mvn install`/`test` must never require a GPG key. Only
       `mvn deploy -Prelease` (what an actual Central publish needs)
       activates them. synthforge-demo and remitflow never carry this
       profile at all. -->
  <profiles>
    <profile>
      <id>release</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-source-plugin</artifactId>
          </plugin>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-javadoc-plugin</artifactId>
          </plugin>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-gpg-plugin</artifactId>
          </plugin>
          <plugin>
            <groupId>org.sonatype.central</groupId>
            <artifactId>central-publishing-maven-plugin</artifactId>
          </plugin>
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>flatten-maven-plugin</artifactId>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
