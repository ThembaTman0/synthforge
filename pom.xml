<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.github.ThembaTman0</groupId>
  <artifactId>synthforge-parent</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>SynthForge</name>
  <description>A JPA-aware fake data seeding library for Spring Boot.</description>
  <url>https://github.com/ThembaTman0/synthforge</url>

  <!-- Metadata required by Maven Central's publish validation (M4 gate,
       not met yet, but cheap to have correct ahead of time). Only
       synthforge-core and synthforge-spring will ever actually publish;
       synthforge-demo and remitflow are maven.deploy.skip=true. -->
  <licenses>
    <license>
      <name>MIT License</name>
      <url>https://opensource.org/licenses/MIT</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <developers>
    <developer>
      <id>ThembaTman0</id>
      <url>https://github.com/ThembaTman0</url>
    </developer>
  </developers>

  <scm>
    <connection>scm:git:https://github.com/ThembaTman0/synthforge.git</connection>
    <developerConnection>scm:git:git@github.com:ThembaTman0/synthforge.git</developerConnection>
    <url>https://github.com/ThembaTman0/synthforge</url>
    <tag>HEAD</tag>
  </scm>

  <!--
    Standalone reactor build. RemitFlow (remitflow/) joined as a sibling
    module rather than SynthForge moving into it. See
    synthforge-v1-spec.md section 5 and remitflow-v1-spec.md.
  -->
  <modules>
    <module>synthforge-core</module>
    <module>synthforge-spring</module>
    <module>synthforge-demo</module>
    <module>remitflow</module>
  </modules>

  <properties>
    <java.version>21</java.version>
    <maven.compiler.source>21</maven.compiler.source>
    <maven.compiler.target>21</maven.compiler.target>
    <!-- Needed for Spring MVC to resolve unnamed @PathVariable/@RequestParam
         by reflection (e.g. remitflow's @PathVariable Long id) instead of
         requiring every one to be spelled out explicitly. -->
    <maven.compiler.parameters>true</maven.compiler.parameters>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <!-- Spring Boot 4.1.0 was current as of July 2026. Check for a newer
         patch release before you build for real. -->
    <spring-boot.version>4.1.0</spring-boot.version>
    <!-- Datafaker 2.7.0 was current as of July 2026. -->
    <datafaker.version>2.7.0</datafaker.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>
</project>
